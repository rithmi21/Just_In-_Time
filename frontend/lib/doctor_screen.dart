import 'package:flutter/material.dart';

class DoctorScreen extends StatelessWidget { // Change to ClinicScreen, NavScreen etc.
  const DoctorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Empty Page Template")),
    );
  }
}