import 'package:flutter/material.dart';

class NavigationScreen extends StatelessWidget { // Change to ClinicScreen, NavScreen etc.
  const NavigationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Empty Page Template")),
    );
  }
}
