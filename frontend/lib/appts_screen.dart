import 'package:flutter/material.dart';

class ApptsScreen extends StatelessWidget { // Change to ClinicScreen, NavScreen etc.
  const ApptsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Empty Page Template")),
    );
  }
}