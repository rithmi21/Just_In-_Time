import 'package:flutter/material.dart';

class ClinicScreen extends StatelessWidget { // Change to ClinicScreen, NavScreen etc.
  const ClinicScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Empty Page Template")),
    );
  }
}