import 'package:flutter/material.dart';

class QueueScreen extends StatelessWidget { // Change to ClinicScreen, NavScreen etc.
  const QueueScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Empty Page Template")),
    );
  }
}